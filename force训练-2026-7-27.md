# Stamp Seal Force 训练 — V1 复盘 & V2 方案

> 日期：2026-07-27  
> 项目：`openpi-force` Pi0Force Dual-Head  
> 任务：Stamp Seal（盖章）  
> 模型：Pi05 + LIMoE (4 experts, top-1) + Force Head

---

## 1. V1 训练回顾

### 1.1 基本信息

| 项目 | 值 |
|------|-----|
| Config | `pi05_force_stamp_seal_remote` |
| Exp name | `stamp_seal_force_v1` |
| 数据集 | `/data/group1/junjie008/datasets/stamp_seal_v2_flexiv` |
| 模式 | 全新训练 (pi05_base)，无 resume |

### 1.2 训练日志文件

| 文件 | 内容 | 步数 |
|------|------|------|
| `stamp_seal_force_378.out` (1.1MB) | 首次训练 | Step 0 → 27930 |
| `stamp_seal_force_381.out` (536KB) | Resume from ckpt 26000 | Step 28010 → 42070 |

### 1.3 关键事件时间线

```
Step 0-500:    正常训练，force_loss 从 3.9 收敛到 0.68
Step 500-24000: MoE router_z_loss 缓慢爬升 (4.5 → 102.5)
Step 25000:     router_z_loss = 168.9（72% token 被丢弃）
Step 25230:     峰值 router_z_loss = 312
Step 25540:     自我恢复到 66.7
Step 26000:     保存 checkpoint，router_z_loss ≈ 122，expert 0/1 均衡
Step ~27930:    首次训练停止
--- Resume from ckpt 26000（用户调低 LR）---
Step 28010:     router_z_loss = 313（继承崩溃状态）
Step 34350:     router_z_loss = 450（加速崩溃）
Step 42060:     router_z_loss = 625（完全崩溃）
```

### 1.4 Expert 分布演变

| 步数 | router_z_loss | E0 | E1 | E2 | E3 |
|------|--------------|------|------|------|------|
| 26000 | 122 | 44% | 55% | 1% | 0.3% |
| 28010 | 313 | 59% | 40% | 0.3% | 0.9% |
| 32090 | 302 | 70% | **0.2%** | 30% | 0.2% |
| 42060 | 625 | 34% | 0.4% | 18% | **47%** |

**Expert 1 在 31000 步死亡，Expert 3 最终接管。**

### 1.5 实机测试结果

| Checkpoint | 来源 | 实机表现 |
|-----------|------|---------|
| 26000 | 首次训练 | ✅ 正常工作（能看到远处目标并移动） |
| 30000 | resume 后 | ❌ 振荡，卡初始位姿 |
| 40000 | resume 后 | ❌ 完全崩溃 |

---

## 2. 根因分析

### 2.1 为什么降 LR 导致崩溃？

1. **aux_loss 只监控不参与训练**，没有机制防止 router 把所有 token 发给同一个 expert
2. **高 LR 提供噪声**：router 更新量大，token 偶尔分配到其他 expert，维持平衡
3. **降 LR → 路由冻结**：更新量变小，路由决策变得更"硬"，router_z_loss 反而加速增长
4. **Death Spiral**：
   ```
   Expert 0 多拿 token → Expert 0 梯度更好 → Router 更偏 Expert 0
   → Expert 1 少拿 token → Expert 1 更"生锈" → 彻底死亡
   ```

### 2.2 其他因素排除

| 因素 | 判断 | 依据 |
|------|------|------|
| 数据集问题 | ❌ 不是 | action_loss 和 force_loss 一直很低（~0.005, ~0.07） |
| Resume 时机 | ⚠️ 部分 | 从已半崩溃的 ckpt 26000 resume，错过了 24000→25540 的自我修复期 |
| LR 调整 | 🔴 主因 | 降 LR 消除了 router 的隐式正则化（噪声），加速了 expert 死亡螺旋 |

---

## 3. V2 方案

### 3.1 核心改动：Router 与 Expert LR 解耦

**修改文件**：`src/openpi/training/optimizer.py`

```
V1（旧）: 整个 limoe（Router + Expert + 共享层）统一 5x LR
V2（新）: RouterWeights → base (1x)，Expert → new (5x)
```

```python
# 新增分区规则
router_pattern = re.compile(r".*RouterWeights.*")  # → base (1x)
high_lr_pattern = re.compile(r".*limoe.*|.*force.*|.*state_proj.*")  # → new (5x)
```

### 3.2 LR 分配表

| 分组 | 模块 | LR | 原理 |
|------|------|-----|------|
| base (1x) | PaliGemma, action_proj, time_mlp, **RouterWeights** | **5e-5** | Router 慢更新 → z_loss 不爆 |
| new (5x) | limoe MlpBlock_0 (4 experts), limoe 共享层, force, state_proj | **2.5e-4** | Expert 快更新 → 防"生锈" |

### 3.3 训练配置

| 参数 | 值 |
|------|-----|
| Exp name | `stamp_seal_force_v2` |
| 模式 | `--overwrite`（全新，不 resume） |
| Batch size | 32 |
| Steps | 50000 |
| LR Schedule | CosineDecay, peak=5e-5, warmup=5000, flat |
| Save interval | 2000 |
| Keep period | 10000（保留 ~6 个，约 300GB） |
| num_experts / top_k | 4 / 1 |

### 3.4 SBATCH 提交

```bash
# 远端
squeue -u junjie008           # 检查现有 job
sbatch /data/group1/junjie008/openpi-force/job_stamp_seal_force.sbatch
tail -f /data/group1/junjie008/openpi-force/logs/stamp_seal_force_<jobid>.out
```

---

## 4. 文件传输清单

### 上传到远端（sftp）

| 文件 | 远端路径 |
|------|---------|
| `src/openpi/training/optimizer.py` | `/data/group1/junjie008/openpi-force/src/openpi/training/optimizer.py` |
| `job_stamp_seal_force.sbatch` | `/data/group1/junjie008/openpi-force/job_stamp_seal_force.sbatch` |

### 不需要上传

- `config.py` — 未修改
- `norm_stats.json` — 已有
- 数据集 — 已有

---

## 5. V2 监控要点

| 指标 | 期望值 | 警戒线 |
|------|--------|--------|
| force_loss (step 0) | ~3.9 | — |
| router_z_loss (step 0) | ~5-6 | — |
| router_z_loss (中后期) | **低于 V1 同期** | >100 需关注 |
| fraction_left_behind | <0.55 健康 | >0.65 危险 |
| 各 expert 占比 | 均匀（~25% 左右） | 某 expert <2% |

---

## 7. V2 当前状态

**正在进行中**（已提交 SLURM）。

| 项目 | 值 |
|------|-----|
| 提交时间 | 2026-07-27 18:07 UTC |
| Job ID (PID) | 2595385 |
| 监控 | `tail -f /data/group1/junjie008/openpi-force/logs/stamp_seal_force_<jobid>.out` |
| WandB | 可用，监控 router_z_loss + expert 分布 |

### 7.1 V1 vs V2 早期对比（Step 0-570）

| 指标 | V1 @ Step 570 | V2 @ Step 570 | 趋势 |
|------|:--:|:--:|:--:|
| action_loss | 0.0530 | 0.0505 | ≈ 一致 |
| force_loss | 0.6310 | 0.5753 | ≈ 一致 |
| router_z_loss | **4.48** | **4.40** | ≈ 一致 |
| E0 占比 | 12% | 7% | V2 略低 |
| E1 占比 | 51% | 59% | V2 略高 |
| E2 占比 | 33% | 31% | ≈ 一致 |
| E3 占比 | 4% | 3% | ≈ 一致 |
| fraction_left_behind | 58% | 60% | ≈ 一致 |

> **结论**：V2 与 V1 在早期完全相同，LR 改动的效果需要到 2000 步以后（V1 开始出现 router_z_loss 爬升）才能观测到。

### 7.2 效果判断标准

**V2 如果成功（router_z_loss < 50，expert 分布均匀）：**
→ V2 即为生产方案，后续可以平滑提升到 50000 步

**V2 如果仍然崩溃（router_z_loss > 100，expert 死亡）：**
→ 执行方案 A（2 expert），见 §8

---

## 8. 后续方案：MoE 架构简化

### 8.1 背景

FORCE-VLA 论文使用 MoE 的动机是"多模态推理分离"——不同 expert 处理 action 推理和 force 推理。但从 V1 实际训练来看：

- Expert 0+1 包揽了 **99%** 的 token
- Expert 2+3 完全死亡（<1%）
- "多模态分离"并未真正发生

这说明在 Pi05 规模下，4 expert 的稀疏路由可能过度参数化。

### 8.2 当前 IMoE 结构

```
LIMoEBlock (dim=2048):
  ├── Encoder1DBlock      ← Self-Attention + MLP (dim=2048)
  ├── MoeLayer            ← 4 experts, top-k=1, 每expert=单层FFN
  └── Dense(out=1024)     ← 输出投影
```

### 8.3 方案对比

| | 当前 (4 experts) | 方案A: 2 expert | 方案B: 纯 Dense |
|------|:--:|:--:|:--:|
| Expert 数 | 4 | 2 | 0 (无路由) |
| 总参数量 | ~64M | ~64M | ~32M |
| 崩溃风险 | 高（V1 实证） | 低 | 无 |
| Router z_loss | 有（会爆炸） | 有但更慢 | 无 |
| 改动成本 | - | config 一行 | 改 limoe.py |
| FORCE-VLA 对齐 | ✅ | ⚠️ 变体 | ❌ |

#### 方案 A: `num_experts=2`

```
改动: config.py 中 num_experts=4 → 2
```

- Router 从 4-class 变 2-class，z_loss 增长更慢
- 2 选 1 下，死亡一个 expert = 模型完全失效，立即暴露，不会像 4 expert 那样"偷偷死"
- V1 中 expert 0+1 已处理 99% token，2 expert 本质上等价于 V1 的有效行为

#### 方案 B: 纯 Dense（无 MoE）

```
改动: limoe.py 中删除 MoeLayer，改为单层 MlpBlock
```

- 零崩溃风险，训练最简单
- 参数量减半
- 偏离 FORCE-VLA 设计，但既然 expert 在实际训练中已经退化到约等于 dense，去掉路由反而是"承认现实"

### 8.4 执行决策树

```
V2 训练结果
├── router_z_loss < 50, expert 分布均匀
│   └── ✅ V2 即为最终方案，保持 4 expert
├── router_z_loss > 100, 但 expert 0+1 仍活跃
│   └── 方案 A: 切 2 expert，降低路由复杂度
└── router_z_loss > 100, 且 1 expert 独大
    └── 方案 B: 去掉 MoE，改纯 Dense
```

---

## 9. 架构补充：VLM → IMoE → Head 数据流

```
VLM prefix_out ──┐
                 ├── Concat ──→ LIMoE (SA + MoE + 残差 + Dense)
Force tokens ────┘                    │
                           取最后 H=30 个 token
                                      │
         suffix_out (action expert, 最后 H 位置) ──┤
                                      │
                                 shared = 相加
                                 ┌─────┴─────┐
                            action_out_proj  force_out_proj
                                 │               │
                            action_pred     force_pred
```

- LIMoE 输出与 suffix_out 相加 → **联合 latent**（shared）
- 两个 head 共享此 latent，分别预测 action 和 force
- 梯度路由（three_stage）：force loss 不反向传到 VLM，但可传到 LIMoE

---

## 10. 模型训练评价方式

### 10.1 训练阶段（WandB / 日志监控）

| 指标 | 含义 | 健康范围 | 警戒线 |
|------|------|---------|--------|
| `action_loss` | 动作流匹配 loss | < 0.01（收敛后） | 不收敛 |
| `force_loss` | 力预测 Huber loss | < 0.1（收敛后） | 不收敛或过早 > 1.0 |
| `moe_router_z_loss` | Router logits 平方和 | **< 10** | **> 50** |
| `moe_fraction_left_behind` | 未被选中的 token 比例 | **< 0.55** | **> 0.65** |
| `moe_expert_usage` | 有效 expert 数（entropy） | > 0.35 (4 expert) | < 0.25 |
| `moe_top1_frac_expert_N` | 各 expert 的 top-1 token 占比 | 各 ~25%（均匀） | 某 expert < 2% |
| `grad_norm` | 梯度范数 | 0.3~5.0（收敛后） | > 50 或 → 0 |
| `param_norm` | 参数范数 | 稳步缓慢增长 | 暴涨（数值不稳定） |
| `skipped_nonfinite` | 因 NaN/Inf 跳过的更新 | 0 | > 0 |

### 10.2 推理阶段（实机/仿真测试）

#### 10.2.1 定量评价

| 指标 | 方法 | 工具 |
|------|------|------|
| **成功率** | 任务完成次数 / 总尝试次数 | rollout 脚本 + 人工判定 |
| **平均完成时间** | 成功 episode 的步数 × 控制频率 | rollout 日志 |
| **Force tracking error** | ‖force_pred − force_gt‖ 均值 | `force_log.json` |
| **Action smoothness** | 相邻帧 action 差的 L2 范数 | rollout 日志 |

#### 10.2.2 定性评价

| 指标 | 方法 |
|------|------|
| **轨迹自然度** | 观察机器人运动是否平滑，有无抖动/振荡 |
| **Force 响应** | 观察接触工件时力控是否及时调整，是否有过冲 |
| **泛化性** | 不同初始位姿下是否都能完成任务 |
| **Failure mode** | 记录失败原因：卡住/振荡/力控过度/力控不足/完全不动 |

#### 10.2.3 Checkpoint 选择流程

```
1. 从 WandB/日志找到 router_z_loss < 50 且 expert 分布均匀的 step 区间
2. 每 2000 步取一个 checkpoint 做实机测试
3. 每个 checkpoint 跑 5-10 个 episode
4. 综合成功率和 force tracking 选出最佳 checkpoint
```

### 10.3 多 checkpoint 对比矩阵

基�chstone 任务：Stamp Seal（盖章）

| Checkpoint | action_loss | force_loss | router_z_loss | 成功率 | Force 响应 | 轨迹质量 |
|-----------|------------|-----------|--------------|--------|-----------|---------|
| 26000 (V1) | ~0.005 | ~0.09 | ~122 | ✅ 可工作 | 可 | 可 |
| 30000 (V1) | ~0.005 | ~0.08 | ~313 | ❌ 振荡 | 差 | 卡初始 |
| 40000 (V1) | ~0.005 | ~0.06 | ~480 | ❌ 崩溃 | 无 | 不动 |
| V2 待测试 | — | — | — | — | — | — |
